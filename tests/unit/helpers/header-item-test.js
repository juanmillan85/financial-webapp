import {
  headerItem
} from 'financial-webapp/helpers/header-item';

module('HeaderItemHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = headerItem(42);
  ok(result);
});
