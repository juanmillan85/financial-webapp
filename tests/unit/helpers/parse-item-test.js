import {
  parseItem
} from 'financial-webapp/helpers/parse-item';

module('ParseItemHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = parseItem(42);
  ok(result);
});
