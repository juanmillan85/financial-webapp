import {
  imageProfile
} from 'financial-webapp/helpers/image-profile';

module('ImageProfileHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = imageProfile(42);
  ok(result);
});
